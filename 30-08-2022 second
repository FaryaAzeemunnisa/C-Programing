#include <stdio.h>
#include<string.h>
int main() {
    char s[100];
    gets(s);
    int i;
    for(i=0;s[i]!='\0';i++){
        if(s[i]=='a'){
            s[i]=toupper(s[i]);
        }
        printf("%c",s[i]);
    }
    return 0;
}
