#include <stdio.h>
int main() {
    int a[100];
    int i,n;
    printf("enter n");
    scanf("%d",&n);
    int x=0;
    int y=1;
    for(i=0;i<=5;i++)
    {
        a[i]=x+y;
        printf("%d\n",a[i]);
        x=y;
        y=a[i];
    }
    return 0;
}
