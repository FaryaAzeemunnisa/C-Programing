#include <stdio.h>
int main() {
    char str[100];
    gets(str);
    int i,count=0;
    for(i=0;str[i]!='\0';i++){
        if(str[i]==' ')
        count++;
    }
    printf("the total count of words are:%d",count+1);
    return 0;
}
